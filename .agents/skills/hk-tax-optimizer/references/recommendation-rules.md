# Recommendation Rules

Every estimated saving must be calculated by re-running the full tax engine before and after an opportunity.

Use statuses: `ELIGIBLE`, `POSSIBLY_ELIGIBLE`, `REQUIRES_CONFIRMATION`, `NOT_ELIGIBLE`, `ALREADY_MAXIMIZED`.
